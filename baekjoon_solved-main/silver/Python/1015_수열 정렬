from collections import deque
INF = int(1e9)
n = int(input())
result = [0] * n
array = list(map(int,input().split()))
p = deque([i for i in range(n)])
while p:
    min_value = min(array)
    for i in range(len(array)):
        if min_value == array[i]:
            result[i] = p.popleft()
            array[i] = INF
print(" ".join([str(i) for i in result]))
##리스트 양옆 기호 [,] 없애고 출력 가능
##힙에 값,인덱스 저장후 heapq.heappop() 사용한 풀이도 가능
