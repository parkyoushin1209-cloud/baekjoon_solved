n = int(input())
dp = [0] * (n+1)
d = [0] * (n+1)
d[0] = 0
for i in range(1,n+1):
    d[i] = int(input())
dp[0] = 0
dp[1] = d[1]
if n >= 2:
    dp[2] = d[1] + d[2]
for i in range(3,n+1):
    dp[i] = max(d[i] + dp[i-2], dp[i-3] + d[i-1] + d[i])
print(dp[n])

##위에서부터 내려오면서 한 칸 내려오면 다음 칸은 두 칸내려와야 함 이후 두칸 내려온 경우와 마찬가지로 두 가지 선택 가능
##바텀업 방식으로 안 했음 + dp랑 계단 값을 따로 설정 못함
##인덱스 에러의 경우 -> 입력되는 수의 개수와 dp의 인덱스 비교해보기
