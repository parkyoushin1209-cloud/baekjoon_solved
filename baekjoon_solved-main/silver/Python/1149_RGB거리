n = int(input())
d = [[0,0,0]]
for _ in range(n):
    d.append(list(map(int,input().split())))

for i in range(2,n+1):
    d[i][0] += min(d[i-1][1],d[i-1][2])
    d[i][1] += min(d[i-1][0],d[i-1][2])
    d[i][2] += min(d[i-1][0],d[i-1][1])
print(min(d[n]))
##각 단계에서 가능한 모든 색 선택 케이스별 최소 비용을 DP 리스트에 저장
