from collections import deque
n, k = map(int,input().split())
q = deque([i for i in range(1,n+1)])
i = 0
while q:
    for _ in range(k-1):
        q.append(q.popleft())
    print(q.popleft())

## k번째 사람 제거 visited 사용없이 반복문과 큐 사용시 가독성 올라감
## 원형 순환 -> 큐 이용 가능
