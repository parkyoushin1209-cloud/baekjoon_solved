import sys
input = sys.stdin.readline
n, m = map(int,input().split())
data = list(map(int,input().split()))

nu = [0] * (n+1)
nu[1] = data[0]
for i in range(2,n+1):
    nu[i] = nu[i-1] + data[i-1]

cnt = [0] * m
for i in range(n+1):
    cnt[nu[i] % m] += 1

total = 0
for r in range(0,m):
    total += cnt[r] * (cnt[r]-1) // 2

print(total)
## [nu[j] - nu[i-1]] % M == 0 -> nu[i]를 M으로 나눈 나머지 저장 후 조합 이용
